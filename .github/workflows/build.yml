name: Build macOS Application

on:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: macos-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pyinstaller
        pip install -r requirements.txt

    - name: Build Mac App
      run: |
        # Собираем напрямую main.py, Стримлит сам откроет браузер при запуске программы
        pyinstaller --noconfirm --onedir --windowed \
          --name "FinaKostantin" \
          --icon="assets/icon.icns" \
          --add-data "main.py:." \
          --add-data "logic:logic" \
          --add-data "data:data" \
          --add-data "assets:assets" \
          main.py

    - name: Sign App
      run: |
        codesign --force --deep --sign - dist/FinaKostantin.app

    - name: Compress App
      run: |
        cd dist
        zip -r FinaKostantin-Mac.zip FinaKostantin.app

    - name: Upload Finished App
      uses: actions/upload-artifact@v4
      with:
        name: FinaKostantin-Mac-SuperGift
        path: dist/FinaKostantin-Mac.zip
